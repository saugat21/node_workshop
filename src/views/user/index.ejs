<html>

<head>
    <title>user</title>

</head>

<body>
    <h1>Usesr page</h1>
    <table class="table">
        <thead>
            <tr>
                <th>sn</th>
                <th>Name</th>
                <th>Address</th>
                <th>Created at</th>

                <th>Updated at</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <% list.forEach((item,index)=>{%>
                <tr>
                    <td>
                        <%= index+1 %>
                    </td>
                    <td>
                        <%= item.name %>
                    </td>
                    <td>
                        <%= item.email %>
                    </td>
                    <td>
                        <%= item.address %>
                    </td>
                    <td>
                        <%= item.createdAt %>
                    </td>
                    <td>
                        <%= item.updateddAt %>
                    </td>

                </tr>

                <% })%>
        </tbody>
    </table>
</body>

</html>